include_directories(${PROJECT_SOURCE_DIR})

set(DBTEST_SOURCES main.cpp)

set(DBTEST_LINK_LIBRARIES
  ColViz
  ${PROTOBUF_LIBRARIES}
  ${LIBZMQ_LIBRARY}
  ${MYSQLCONNECTORCPP_LIBRARY}
  ${NETT_LIBRARIES})

common_application(dbTest NOHELP)
